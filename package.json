{
  "name": "tymlee",
  "version": "0.1.0",
  "private": true,
  "description": "Text-only time tracker. Static files, no build step.",
  "scripts": {
    "test": "node --test",
    "start": "python3 -m http.server 8000"
  }
}
